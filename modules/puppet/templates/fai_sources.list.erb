# Build the nfsroot with the first apt source
# Using the apt cacher server may cause problem if the server is not ready

# debian 
deb <%= @apt_debian_reps[0] %> <%= @fai_distribution %> main contrib non-free

# security #
deb http://security.debian.org/debian-security <%= @fai_distribution %>-security main contrib non-free
